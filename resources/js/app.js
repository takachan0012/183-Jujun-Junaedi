import './bootstrap';
import {
    togglePasswordVisibility,
    listCustomerDebt,
    listMyDebt,
    navigationBack
} from './script';

window.togglePasswordVisibility = togglePasswordVisibility;
window.listCustomerDebt = listCustomerDebt;
window.listMyDebt = listMyDebt;
window.navigationBack = navigationBack;